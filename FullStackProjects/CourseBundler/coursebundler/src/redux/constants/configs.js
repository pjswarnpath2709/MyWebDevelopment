export const RequestJsonConfig = {
  headers: {
    'Content-Type': 'application/json',
  },
  withCredentials: true,
};

export const RequestCredentialsConfig = {
  withCredentials: true,
};

export const RequestFormConfig = {
  headers: {
    'Content-Type': 'multipart/form-data',
  },
  withCredentials: true,
};
